#include <bits/stdc++.h>
using namespace std;

#define _D(...) printf(__VA_ARGS__)
#define FOR(i,a,b) for(int i=(a); i<(b); ++i)
typedef long long ll;

void solve_1244() {
    int N; scanf("%d", &N);
    FOR(i,0,N) {
        
    }
}

int main() {
    int TC = 1;
    // scanf("%d", &TC);
    while(TC--) solve_1244(); 
    return 0;
}

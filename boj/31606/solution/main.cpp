#include <bits/stdc++.h>
using namespace std;

#define _D(...) printf(__VA_ARGS__)
#define FOR(i,a,b) for(int i=(a); i<(b); ++i)
typedef long long ll;

void solve_31606() {
    int N, M; scanf("%d %d", &N, &M);
    printf("%d\n", N + M + 3);
}

int main() {
    int TC = 1;
    // scanf("%d", &TC);
    while(TC--) solve_31606(); 
    return 0;
}
